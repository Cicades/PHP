<?php 
$arr = array('key' =>'value');
//isset($input) 判断是否存在某个值，存在则返回true，否则就返回false
if (isset($arr['key'])) {
	echo "存在";
}
echo "<br>";
//empty($input) 如果不存在则返回真，相当于isser（）取反
if (!empty($arr['key'])) {
	echo "存在";
}
//拓展 "0"==false 返回的是true ，因为进行比较时实际上是将两端的数据转换为数字进行比较，"0"转换为0，false转换为0